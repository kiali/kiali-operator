# Project requirements
# 'pip install -r requirements.txt'
# In addition to these, you must also exec "ansible-galaxy collection install -r ../requirements.yml"
molecule
jmespath
junit-xml
kubernetes
docker

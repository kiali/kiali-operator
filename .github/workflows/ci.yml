name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-20.04
    steps:
      - name: Check out code
        uses: actions/checkout@v2
      - name: Validate OLM metadata
        run: make validate
      - name: Validate OLM metadata
        run: |
          kind create cluster
          make validate-cr
          kind delete cluster
      - name: Build operator
        run: make build
